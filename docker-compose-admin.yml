version: '3.8'

services:
  admin:
    build: ./safetyshoe-admin
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=https://dtwz.zhiyuansafety.com
      - NEXT_PUBLIC_ADMIN_URL=https://dtwz.zhiyuansafety.com
    restart: unless-stopped
